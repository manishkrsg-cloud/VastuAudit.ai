{
  "name": "vastuaudit-prototype",
  "version": "1.0.0",
  "description": "VastuAudit.ai design prototype — static site deployed to Railway",
  "private": true,
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}",
    "dev": "serve -s . -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
